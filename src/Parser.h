#pragma once
#include <iostream>
#include <string>
#include <vector>

class Parser {
public:
    Parser(std::vector<std::vector<std::string>> TokenizedSource,
           std::vector<std::vector<std::string>> TokensInfo);
};
